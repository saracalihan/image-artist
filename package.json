{
  "name": "image-artist",
  "version": "1.0.0",
  "description": "Pure HTML element for mark image or draw shapes on it",
  "main": "dist/script.js",
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "start": "tsc --watch",
    "build": "tsc",
    "serve": "cp -r ./example/* ./dist && http-server ./dist -c-1 -d"
  },
  "author": "Alihan SARAÇ <saracalihan@gmail.com>",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "http-server": "^14.1.1",
    "typescript": "^5.0.2"
  }
}
